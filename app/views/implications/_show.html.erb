<% (implication.atoms - [implication.atoms.last]).each do |a| %>
  <%= render partial: "atoms/inline_atom",
             locals: { atom: a,
                       delim_open: '(',
                       delim_close: ')',
                       fullstop:'' } %>
  <%= content_tag(:strong, t('implications.and')) %>
<% end %>
<%= render partial: "atoms/inline_atom",
           locals: { atom: implication.atoms.last,
                     delim_open: '(',
                     delim_close: ')',
                     fullstop: '' } %>
$\Rightarrow$
<%= render partial: "atoms/inline_atom",
           locals: { atom: implication.implies } %>